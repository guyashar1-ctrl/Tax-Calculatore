# שלב 0 — צילום מצב לפני מהלך "המסע הוא הכרטיס"

> נכתב 2026-08-05 בפתיחת `docs/PLAN-JOURNEY-CENTER.md`. ענף: `feat/journey-center`.
> אין כאן שינוי קוד — רק אימות מול המציאות החיה.

## 1. מסד הנתונים החי

פרויקט `uoweoqtuiettozagwgdw`. מיגרציה אחרונה `20260804191955`
(`long_tail_steps_dont_block_activation_55b`), 83 מיגרציות בסך הכול.
`public_link_health()` **ירוק**: sign 6/6 · quote 3/3 · intake 2/2 · onboard 10/10.

| ישות | כמות |
|---|---|
| לקוחות | 17 (14 פעילים · 3 בקליטה · 0 בהצעה · 0 לידים · 0 ארכיון) |
| התקשרויות | 3 — כולן בסטטוס `onboarding`, כולן מפורסמות |
| שלבי קליטה | 28 (כולם פתוחים) |
| אירועי קליטה | 12 |
| בקשות ייצוג | 10 · הצעות 4 · לידים 3 · משימות 14 · מסמכים 11 |

חלוקה לפי פרופיל:

| פרופיל | לקוחות | התקשרויות | שלבים | הערה |
|---|---|---|---|---|
| `guy@yasharcpa.co.il` | 5 | **3** | **28** | נתוני אמת — כל הקליטה החיה כאן |
| `e2e-test@firm.local` | 5 | 0 | 0 | משתמש הבדיקה — פעיל ומאושר |
| `dev@local.test` | 7 | 0 | 0 | לקוחות הדוגמה של שרת הפיתוח |

שלוש ההתקשרויות האמיתיות: אילן סימנטוב (7 שלבים) · יובל גרוסמן (9) · שמוליק הבודק (10).

## 2. שבעה פערים שנמצאו

1. **הארכיון המקומי של הפונקציות מיושן.** `supabase/live-2026-08-04/` נכתב
   ב-18:28–18:35 UTC, ומיגרציות 51–55b רצו אחריו (18:42–19:19). כלומר העותקים של
   `get_client_portal`, `generate_onboarding_steps` ו-`advance_onboarding_step` שם
   אינם מה שרץ, ו-`portal_submit_step` / `publish_onboarding_process` /
   `close_onboarding` חסרים לגמרי. **טופל:** נוצרה `supabase/live-2026-08-05/`
   עם הנוסח החי.

2. **למשתמש הבדיקה אין קליטה בכלל** — 0 התקשרויות, 0 שלבים. כל QA של הקליטה
   מחייב יצירת נתוני בדיקה עליו קודם. אי אפשר "לבדוק על מה שיש".

3. **שני סוגי הבקשות שנולדו במיגרציה 51 מעולם לא רצו על נתוני אמת.**
   `client_documents` ו-`prev_accountant_details` — אפס שורות בכל המסד. המרכיב
   יוצר אותם רק בהתקשרות חדשה, ושלוש ההתקשרויות האמיתיות נוצרו לפני 51. מכאן
   שהטפסים בדף האישי (מיגרציה 53b) לא נראו מעולם בעין. תואם את מה שנרשם
   ב-`ONBOARDING-JOURNEY-STATUS.md`.

4. **"לקוח פעיל בלי התקשרות" הוא הרוב, לא מקרה קצה.** 14 לקוחות פעילים מול 3
   התקשרויות. דף המסע חייב להיראות נכון גם כשאין קליטה ואין היסטוריית הצעה —
   זה המסך שגיא יראה בפועל אצל רוב הלקוחות שלו.

5. **`onboarding_steps` חסר `sort_order`** — הסדר היום הוא `created_at` בלבד
   (גם במרכיב, גם בבונה, גם ב-`get_client_portal`). נוסיף עמודה בשלב 1.

6. **`step_type` מוגן ב-CHECK constraint** עם 16 ערכים. הוספת `custom_request`
   מחייבת הרחבת האילוץ (הרחבה בלבד — לא הסרת ערך קיים).

7. **`CLAUDE.md` §5.1 מיושן**: כתוב שם ש-`package.json` נמחק. הוא קיים, עם
   `build: tsc && vite build`. אעדכן את הסעיף בסוף המהלך.

## 3. אימות עוגני ה-UI — כולם במקומם

| עוגן | מיקום שאומת |
|---|---|
| ניווט ראשי (3 טאבים) | `src/App.tsx:1414-1418` |
| מתג `onboardingTab` | `src/App.tsx:235-237` |
| לשוניות כרטיס הלקוח (5) | `src/components/ClientWorkspace.tsx:44-53` |
| התניית לשונית הקליטה | `src/components/ClientWorkspace.tsx:78-79` |
| `handleRowClick` המשולש | `src/components/ClientList.tsx:380-392` |
| `ClientForm.tsx` | **אין לו שום import חי** — מת, ניתן למחיקה בשלב 6 |
| `MyDesk.tsx` | מיובא רק ב-`App.tsx:62` עבור המסלול היתום `#/desk` |
| bucket הקבצים | `client-documents` (פרטי, בלי הגבלת גודל/סוג ברמת ה-bucket) |
| דגלים בפרופילים | `settings.flags` = null בשלושת הפרופילים ⇒ הקליטה דלוקה כברירת מחדל |

## 4. שתי מלכודות סביבה שאושרו מחדש

- **הפורט של שרת הפיתוח.** 5173 ו-5174 תפוסים; vite עלה על **5175**, בעוד
  שכלי התצוגה דיווח על פורט אחר. תמיד לקרוא את הפורט מ-`preview_logs`.
- **החלונית הפנימית לא מצלמת** ("the Browser pane is not displayed"). כל ה-QA
  הוויזואלי במהלך הזה ייעשה דרך Chrome האמיתי (`claude-in-chrome`).

## 5. סיכון לשלב 9 (פריסה)

אין `vercel.json` ואין תיקיית `.vercel` בריפו — הפריסה מתבצעת דרך חיבור GitHub
בצד Vercel, ולכן `git push` ל-master הוא מה שמפעיל אותה. `master` ו-`origin/master`
מסונכרנים כרגע. אאמת את סטטוס הפריסה בפועל בשלב 9.

## 6. בסיס ויזואלי

האפליקציה עולה ועובדת לפני כל שינוי: מסך המשימות נטען עם 45 משימות ו-7 לקוחות
הדוגמה של `dev@local.test` (שרת הפיתוח מזריק אותם במקום נתוני ה-DB).
