-- ═══════════════════════════════════════════════════════════════════════════
--  18 — הידוק רשימת המורשים לפרודקשן  ·  שלב 1 באבטחה
-- ═══════════════════════════════════════════════════════════════════════════
--  לפי החלטת גיא (2026-07-17): בפרודקשן מותרות רק שתי הכתובות האמיתיות.
--  משתמשי בדיקה/פיתוח נשמרים בטבלה אך מושבתים (active=false) — אין להם גישה
--  לנתוני פרודקשן. השורות נשמרות כדי לאפשר הפעלה חוזרת מבוקרת לצורך בדיקות.
--  שינוי דאטה בלבד; אין שינוי סכימה.
-- ═══════════════════════════════════════════════════════════════════════════

update public.authorized_users set active = true
  where lower(email) in ('guyashar1@gmail.com', 'office@yasharcpa.co.il');

update public.authorized_users set active = false
  where lower(email) in ('guy@yasharcpa.co.il', 'e2e-test@firm.local', 'dev@local.test');
