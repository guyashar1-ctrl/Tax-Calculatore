-- ═══════════════════════════════════════════════════════════════════════════
--  16 — סגירת רשימת הקבצים בדלי הלוגואים הציבורי  ·  שלב 1 באבטחה
-- ═══════════════════════════════════════════════════════════════════════════
--  הדלי firm-logos ציבורי (כתובות הלוגו מוטמעות במיילים ללקוחות — חייב).
--  אך ל-policy הרחב firm_logos_public_read יש SELECT על כל האובייקטים, מה
--  שמאפשר לקבל *רשימה* של כל הקבצים בדלי. גישה לכתובת אובייקט ישירה בדלי
--  ציבורי לא זקוקה ל-policy הזה — לכן מסירים אותו. הלוגואים ימשיכו להיטען
--  דרך getPublicUrl כרגיל; רק היכולת לדפדף ברשימת הקבצים נחסמת.
-- ═══════════════════════════════════════════════════════════════════════════

drop policy if exists firm_logos_public_read on storage.objects;
