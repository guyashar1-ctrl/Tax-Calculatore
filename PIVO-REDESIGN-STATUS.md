# PIVO · מצב העיצוב מחדש

מסמך המשך. נכתב בסוף סשן, כדי שהסשן הבא יתחיל לעבוד ולא לחקור.

## הדבר החשוב ביותר — איפה העיצוב

**התיקייה `כל המסכים ב HTML` היא ייצוא של האפליקציה עצמה, לא מוקאפים.**
`scripts/design-export-server.mjs` מקבל את ה-DOM מהדפדפן ועוטף אותו ב-`src/index.css`.
זה הקלט שנשלח *אל* Claude Design. בנייה מולו = בנייה מול הגרסה הישנה.

**המוקאפים** נמצאים בפרויקט Claude Design `77baf2d1-38fc-4130-874c-ec875af9aedf`
("גישה לקבצי המסכים"), ונקראים בכלי `DesignSync` (method `get_file`).

## עקרון העיצוב

מתוך `export/README.md` של הפרויקט:

1. שינוי עיצוב = CSS. אין שינוי מבנה ואין מחלקות חדשות, אלא אם רשום במפורש.
2. אין טקסט עברי חדש ואין אייקונים חדשים.
3. כל דבר שמוסתר — נרשם: מה, למה, ולאן עבר.
4. **צבע = מידע.** איחור, דחיפות, תקיעות. סוג, קטגוריה וזהות = טקסט.
5. כל שם וכל מספר מופיעים פעם אחת במסך.

## מה כבר נבנה

| מסך | מקור | Commit |
|---|---|---|
| מסגרת האפליקציה (כרטיס צף על קנבס) | `משימות · מינימלי.dc.html` | `e9259d0` |
| משימות | `משימות · מינימלי` + `export/pivo-final.css` | `91e9f0b` |
| לקוחות (10 עמודות → 6) | `export/pivo-clients.css` | `91e9f0b` |
| כרטיס לקוח · מרכז שליטה | `מסך07.dc.html` | `a68696b` |
| הצעות מחיר · מצב ריק | `מסך12.dc.html` | `df7fe15` |
| כרטיס לקוח · משימות | `מסך10.dc.html` | `1c0d728` |

## מה נשאר

| מוקאפ | מסך | קומפוננטה |
|---|---|---|
| `מסך08.dc.html` | כרטיס לקוח · התיק | `clientTabs/ClientDossierTab.tsx` |
| `מסך09.dc.html` | כרטיס לקוח · מסמכים | `clientTabs/DocumentsTab.tsx` |
| `מסך16.dc.html` | דוח 1301 · מפת העץ | `features/annualReport/TreeMapView.tsx` |
| `מסך17.dc.html` | מסד נתוני מס | `features/annualReport/TaxConstantsDashboard.tsx` |
| `מסך18.dc.html` | מרכז ידע מס | `features/taxCenter/TaxCenter.tsx` |
| `מסך24.dc.html` | ביטוח לאומי | `components/NIReferenceSection.tsx` |
| `export/screens/NN-after.html` | 26 מסכים, גרסת "אחרי" | — |

עליהם כבר פועלת שפת העיצוב הגלובלית (כרטיסים שקופים, טאבים בקו תחתון,
תגיות כטקסט) — אבל הפריסה שלהם לא נבנתה מול המוקאפ.

## מחזור העבודה למסך

1. `DesignSync get_file` על `מסךNN.dc.html`. המבנה נמצא ב-`<x-dc>`,
   ונתוני הדוגמה ב-`<script data-dc-script>` בתחתית.
2. לחלץ: רשת (`grid-template-columns`), מרווחים, גדלי גופן ומשקלים.
3. לבנות ב-`src/components/ui/pivo-design.css` תחת כותרת מסומנת,
   ולהוסיף מחלקת שורש לקומפוננטה אם צריך היקף.
4. לאמת בדפדפן: `getComputedStyle` על הרשת, אפס גלילה אופקית,
   סולם גופנים (12/13/14/15/17/20/24/34) ומשקלים (400/500/600).
5. `tsc --noEmit` → `vite build` → commit → push.

## שתי סתירות שהוכרעו

- המוקאפ כולל **נקודת דחיפות** ו**עמודת תיאור** במסך המשימות;
  האפיון מסיר את שתיהן (D5, §4.3). **האפיון גובר** — כך נקבע בסדר העדיפויות.
- המוקאפ מסמן טאב פעיל בצבע ומשקל בלבד; נוסף גם קו תחתון,
  כי צבע לבדו אינו סימון נגיש.

## גבולות שאסור לחצות

`git diff` מול master על הקבצים הבאים חייב להישאר **ריק**:
`src/components/Public*.tsx` · `src/utils/quotationEmailHtml.ts` ·
`src/utils/quotationPdf.ts` · `src/utils/poaPdfGenerator.ts` ·
`src/utils/brandedEmail.ts` · `src/data/`

אלה הדפים שהלקוחות רואים, תבניות המייל, מחוללי ה-PDF ותוכן המס.

## סביבה

- שרת הפיתוח עולה על פורט אחר כש-5173 תפוס — לבדוק ב-`preview_logs`.
- משתמש הבדיקה `e2e-test@firm.local` **חסום לכתיבה** ל-Supabase.
  לכן שמירה, מחיקה ויצירה לא נשמרות — זה לא באג בפיצ'ר.
- שגיאות `useDocumentStore.saveDoc` בקונסול נובעות מכך: `DocumentManager`
  מייצר מסמכי דוגמה ללקוחות `sample-`. קיים מראש.
- נתוני הדוגמה: **45 משימות, 7 לקוחות** (הסף שב-CLAUDE.md מיושן).
