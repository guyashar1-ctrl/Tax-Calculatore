# PIVO · מצב העיצוב מחדש

מסמך המשך. נכתב בסוף סשן, כדי שהסשן הבא יתחיל לעבוד ולא לחקור.

## הדבר החשוב ביותר — מה זה העיצוב הזה

**זו לא שכבת צבע. זו שכבת קומפוזיציה.**

מה שמבדיל את המוקאפים מהמוצר הישן אינו הגוונים אלא הסדר. אפשר להחיל
את כל הטוקנים על מסך ישן, ולקבל מסך ישן בצבעים חדשים. זה מה שקרה
בסבב הראשון, וזה מה שתוקן.

**המוקאפים** נמצאים בפרויקט Claude Design `77baf2d1-38fc-4130-874c-ec875af9aedf`
ונקראים בכלי `DesignSync` (method `get_file`). המבנה ב-`<x-dc>`,
נתוני הדוגמה ב-`<script data-dc-script>` בתחתית.

**התיקייה `כל המסכים ב HTML` היא ייצוא של האפליקציה עצמה, לא מוקאפים.**
בנייה מולה = בנייה מול הגרסה הישנה.

## שכבת המבנה — `.pg-*` ב-`pivo-design.css`

נגזרה ממסך 08 ומסך 18, וחלה על כל מסך במערכת:

| מחלקה | מה זה |
|---|---|
| `.pg` | מעטפת עמוד — קצב 20px בין בלוקים |
| `.pg-head` | כותרת אחת: שם + משפט-מצב אחד משמאל, פעולות מימין, קו מתחת |
| `.pg-split` | מאסטר-דיטייל — מסילה 206/236px, פאנל, מרווח 30–32px |
| `.pg-rail-item` | פריט מסילה — הפעיל מסומן במשטח רך ובמשקל |
| `.pg-fields` | תווית מעל ערך, שתי עמודות, ערך במשטח רך |
| `.pg-bars` | פס התקדמות מקטעי — מקטע לקבוצה |

**שלושה מרווחים בלבד בכל המערכת:** 20 בין בלוקים · 30–32 בין מסילה
לפאנל · 16 בתוך פאנל.

**משפט המצב הוא היחיד שמדווח מספרים.** זה הכלל שמונע את החזרה
שהייתה: מונה בטאב, מונה במחוון, מונה בכותרת הקבוצה — שלוש פעמים
אותו מספר.

## עקרונות (מתוך `export/README.md`)

1. **צבע = מידע.** איחור, דחיפות, תקיעות. סוג, קטגוריה וזהות = טקסט.
2. כל שם וכל מספר מופיעים פעם אחת במסך.
3. אין טקסט עברי חדש ואין אייקונים חדשים.
4. כל דבר שמוסתר — נרשם: מה, למה, ולאן עבר.
5. חצאי-פיקסלים הוצאו משימוש. הסקאלה סגורה: 12·13·14·15·17·20·24·34.

## מה נבנה מחדש מבנית

| מסך | מה השתנה | מקור |
|---|---|---|
| כרטיס לקוח · התיק | שתי קבוצות-ענק מתקפלות → מסילה של 21 קבוצות + פאנל אחד | מסך 08 |
| מרכז ידע מס | סרגל אופקי של 9 כלים → מסילה אנכית; כותרת לכל כלי | מסך 18 |
| מסמכים | כותרת בת 3 שורות + פס מחוונים → שורה אחת; עמודת "קובץ" ירדה | מסך 09 |
| דוח שנתי | 7 טאבים → מסילה, שלבים ממוספרים + קבוצת "כלים" | — |
| טופס לקוח | 9 טאבים → מסילה; פס פעולות דביק ירד (כפילות) | — |
| הצעות ולידים | 4 מחוונים ירדו → משפט מצב; טאבים → מתג | — |
| מחשבון · ייצוג · משרד | `.pg-head` אחיד | — |

## הערכה הכהה

נבנתה כערכה, לא כהיפוך. שתי תקלות תוקנו:
`--s2` ו-`--hairline-2` היו אותו ערך (שדה וקו מפריד נראו זהים);
וכל האפורים נטו לכחול בזמן שהבהירה היא נייר חם.

סולם ההגבהה עכשיו: `#0e0e10` קנבס → `#1b1b1e` כרטיס → `#232326` שדה
→ `#26262b` קו שורה → `#33333a` מפריד מקטע. שמונה מדרגות שונות.

## הצד שהלקוח רואה — מה נגעו ומה לא

הגבול הישן ("diff ריק על כל `Public*`") הוחלף בגבול מדויק יותר, אחרי
שגיא אישר במפורש לעצב גם את הצד שהלקוח רואה.

**מה שנבנה מחדש** — מסכי המצב הציבוריים: "הקישור פג", "החתימה
התקבלה", "טוען", "משהו השתבש". כל עמוד המציא אותם מחדש בכרטיס משלו,
והלקוח עובר בין שלושתם באותו תהליך. עכשיו רכיב אחד, `ClientPageState`.
אלה הרגעים היחידים בלי מיתוג המשרד — "הקישור פג" קורה עוד לפני שידוע
איזה משרד זה — ולכן אין שם מותג, כרטיס או צל.

**מה שלא נגעו בו, ולמה:**

| מה | למה |
|---|---|
| `QuotationWebView` | המסמך שהלקוח מקבל. יושב ב-`.pivo-light`. שינוי בו משנה איך נראות הצעות **שכבר נשלחו ונחתמו** |
| טופס השאלון ועמוד ההצעה | נגזרים מ-`QuotationBrand` — הרו"ח בוחר אותם בסטודיו העיצוב. זו המערכת עובדת נכון, לא חוב |
| `quotationEmailHtml` · `brandedEmail` | מעטפת אחת משותפת ב-`_shared/designSystem.ts`, מונעת מטוקני המותג. אין בה צבע קשיח. שינוי היה **שובר** את ההתאמה בין המייל לעמוד ההצעה |
| `quotationPdf` | מונע מאותם טוקני מותג, ומשקף את המסמך. שינוי היה מרחיק PDF חדש מהצעה שכבר נשלחה |
| `poaPdfGenerator` | כותב על **הטופס הרשמי של רשות המסים** (`poa_2279a5.pdf`) בקואורדינטות קבועות. עיצוב מחדש = טופס פסול |
| `src/data/` | תוכן מס, לא עיצוב |

הכלל שנשאר: **מסמך שנשלח ללקוח — לא נוגעים. מסך מצב של המערכת — כן.**

## איך בודקים בלי צילומי מסך

בסשן הזה חלונית הדפדפן לא הציגה פריימים, ולכן `screenshot` לא עבד.
התחליף שעבד: הזרקת פונקציית ביקורת דרך `javascript_tool` שסורקת את
ה-DOM ומדווחת חריגות מול שפת העיצוב —

- אלמנטים עם מסגרת בכל הצדדים (קופסאות)
- `box-shadow` מחוץ למודאלים
- מידות גופן מחוץ לסקאלה הסגורה
- משקלים מחוץ ל-400/500/600/700
- רקעים שאינם אחד ממשטחי המערכת
- `scrollWidth > clientWidth` (גלילה אופקית)

מסך שנבנה נכון מחזיר אפס בכל הקטגוריות. מה שנשאר מסומן — נקודת
"יש שינוי שלא נשמר", משבצות צבע המותג, פסי גרף, האזהרה בכרטיס
הלקוח — הוא בכוונה, כי שם הצבע הוא-הוא המידע.

בנוסף נמדדו יחסי ניגודיות בשתי הערכות; כולם מעל הסף של WCAG AA.

## סביבה

- שרת הפיתוח עולה על פורט אחר כש-5173 תפוס — לבדוק ב-`preview_logs`.
- משתמש הבדיקה `e2e-test@firm.local` **חסום לכתיבה** ל-Supabase.
  לכן פרופיל המשרד לא נטען, ואי אפשר להתחיל שאלון דוח שנתי.
- מסכים שחסומים נבדקו ברתמה זמנית (`src/__designHarness.tsx` +
  `design.html`) שהרכיבה אותם עם נתוני דמה. **הרתמה נמחקה בסוף.**
  אם צריך שוב — לבנות אותה מחדש, לא להשאיר אותה בעץ.
- מסכי החתימה נבדקים דרך `?test-signroom` · `?test-exec` ·
  `?test-sig` · `?test-repdocs` (קיימים בקוד, DEV בלבד).
- שגיאות `useDocumentStore.saveDoc` בקונסול קיימות מראש.
- נתוני הדוגמה: **45 משימות, 7 לקוחות**.
