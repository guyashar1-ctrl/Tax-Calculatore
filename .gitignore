node_modules/
dist/
.env
.env.local
.env.*.local

# Vite dev cache
.vite/

# Editor / IDE settings (local only)
.vscode/

# Personal study/reference materials — not part of the app
חומרים/
wikisource_raw.txt
מדריך רשות המיסים למילוי דוח שנתי ליחיד (טופס 1301) לשנת 2025.pdf

# תוכן שיווקי שמיוצר בתיקייה הזאת אבל אינו חלק מהאפליקציה
podcast-production/

# Temporary working dirs
tmp_letter/
tmp/

# Claude scratch worktrees (local only)
.claude/worktrees/

.vercel

# מבנה המסד שנמשך מהפרודקשן לבניית ה-staging. נוצר מחדש בכל פעם ע"י
# scripts/staging-schema-extract.mjs, ולכן אינו נשמר בגיט ואינו מתיישן בו.
supabase/staging/schema-from-prod.sql
